{
  "name": "@react-noui/create-form",
  "version": "1.0.2",
  "description": "React form context/provider",
  "main": "lib/index.js",
  "author": "taystack",
  "license": "MIT",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "build": "yarn build:clean; yarn build:types; yarn build:lib",
    "build:types": "yarn tsc",
    "build:lib": "NODE_ENV=production yarn babel src --out-dir lib --source-maps inline --extensions '.ts,.tsx,.js' --ignore '**/__tests__/**' --ignore '**/stories/**'",
    "build:clean": "rimraf lib"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.5",
    "@babel/core": "^7.21.8",
    "@babel/preset-env": "^7.21.5",
    "@babel/preset-react": "^7.18.6",
    "@babel/preset-typescript": "^7.21.5",
    "@types/react": "^18.2.6",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "react": "^18.2.0"
  }
}
